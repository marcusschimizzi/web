---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
    title: string;
    description: string;
    thumbnail: string | ImageMetadata;
    slug: string;
    featured?: boolean;
}

const { title, description, thumbnail, slug, featured = false } = Astro.props;
---

<a href={`/projects/${slug}`} class="group block">
    <div class={`rounded-xl bg-gray-900/50 overflow-hidden border transition-all duration-300 hover:border-blue-500/50 ${featured ? 'border-amber-500/30' : 'border-gray-800'}`}>
        <!-- Featured indicator -->
        {featured && (
            <div class="flex items-center gap-1.5 px-4 py-2 bg-amber-500/10 border-b border-amber-500/20">
                <svg class="w-4 h-4 text-amber-400" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                </svg>
                <span class="text-sm font-medium text-amber-400">Featured</span>
            </div>
        )}

        <!-- Thumbnail -->
         <div class="aspect-video overflow-hidden">
             {typeof thumbnail === 'string' ? (
                 <img
                     src={thumbnail}
                     alt={title}
                     class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
                 />
             ) : (
                 <Image
                     src={thumbnail}
                     alt={title}
                     width={1280}
                     height={720}
                     class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
                 />
             )}
         </div>

        <!-- Content -->
        <div class="p-5">
            <h3 class="text-xl font-bold text-white mb-2 group-hover:text-blue-400 transition-colors">
                {title}
            </h3>
            <p class="text-gray-400 text-sm leading-relaxed">
                {description}
            </p>
        </div>
    </div>
</a>
